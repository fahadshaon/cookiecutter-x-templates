plugins {
    id 'java'
    id 'application'
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'junit:junit:4.13'
}

application {
    mainClassName = System.getProperty("mainClass", "{{cookiecutter.app_package}}.App")
}
