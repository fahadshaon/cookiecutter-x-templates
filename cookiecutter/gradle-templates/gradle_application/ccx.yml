name: gradle_application
description: Create java application managed with gradle
post_gen: |
  - Generate gradle wrapper by running

      gradle wrapper

  ** If the project is a subproject then might need to remove the settings.gradle **
